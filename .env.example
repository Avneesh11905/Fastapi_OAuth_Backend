# Project
FRONTEND_URL="http://localhost:3000"

# Application
PROJECT_NAME="FastAPI OAuth"
DEV=True
# To generate a secure RSA Keypair, run: python scripts/generate_keys.py
# Paste the resulting multi-line strings below:
JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
YOUR_KEY_HERE
-----END PRIVATE KEY-----"

JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----
YOUR_KEY_HERE
-----END PUBLIC KEY-----"

# Security
SESSION_SECRET="your-32-byte-secret-for-oauth-sessions"

# CORS origins (comma separated)
# Note: Do not put localhost dev links here! If DEV=True, localhost:3000, 5173, and 8000 
# are automatically whitelisted. Keeping them out of this variable prevents you from 
# accidentally leaving localhost exposed to Cross-Origin attacks in production.
CORS_ORIGINS="https://myapp.example.com,https://admin.myapp.example.com"

# Caching
# Set to True to use the built-in Python dictionary (great for local dev, no Redis required).
# Set to False to use Redis or your custom Adapter (required for production rate limiting and JWT blacklists).
USE_MEMORY_CACHE=False

# Log retention (days)
LOG_RETENTION_DAYS=28

# SQL Database (PostgreSQL recommended)
DB_ASYNC_URL="postgresql+asyncpg://postgres:password@localhost:5432/auth_db"

# Cache Database
# Used for Rate Limiting, JWT Blacklisting, and API Response Caching.
# Supports redis://, memcached://, etc. Defaults to Redis.
CACHE_URL="redis://localhost:6379/0"

# OAuth Providers
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

GITHUB_CLIENT_ID="your-github-client-id"
GITHUB_CLIENT_SECRET="your-github-client-secret"

# Email Provider Configuration
EMAIL_API_KEY="re_your_api_key_here"
EMAIL_FROM="onboarding@resend.dev"
# Choose a theme: modern, minimal, playful
EMAIL_TEMPLATE_NAME="modern"

# Token Configuration
TOKEN_ACCESS_TOKEN_LIFETIME_MINUTES=15
TOKEN_REFRESH_TOKEN_LIFETIME_DAYS=7
TOKEN_OTP_EXPIRATION_SECONDS=300
TOKEN_OTP_RESEND_WINDOW_SECONDS=900

# Rate Limiting (Gets Enabled at DEV=False)
RATE_LIMIT_REFRESH_RATE_LIMIT="30/minute"
RATE_LIMIT_LOGIN_RATE_LIMIT="5/minute"
RATE_LIMIT_DEFAULT_RATE_LIMIT="60/minute"